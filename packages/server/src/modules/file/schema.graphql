type Query {
  getEntryFile(path: String!, force: Boolean!): String!
}

type File {
  title: String!
  description: String!
  path: String!
  content: String!
}

type Mutation {
  openFile(filePath: String): File!
}
