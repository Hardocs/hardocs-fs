type Query {
  openFile(filePath: String!): File!
  getEntryFile(path: String!, fullPath: Boolean!): String!
}

type File {
  title: String!
  description: String!
  path: String!
  content: String!
}
